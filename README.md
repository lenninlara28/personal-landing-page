This is a [Next.js](https://nextjs.org) project.

## Getting Started

First, run the development server:

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

You can start editing the page by modifying `src/app/page.tsx`. The page auto-updates as you edit the file.

## Build and Export

This project is configured to export as a static site (HTML/CSS/JS). To build the project:

```bash
npm run build
```

The output will be in the `out` directory, which can be hosted on any static web server (like GitHub Pages).

## Learn More

To learn more about Next.js, take a look at the [Next.js Documentation](https://nextjs.org/docs).

